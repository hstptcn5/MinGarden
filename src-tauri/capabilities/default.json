{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "MinGarden desktop permissions",
  "windows": ["garden"],
  "permissions": [
    "core:default",
    "core:window:allow-set-always-on-top",
    "store:default",
    "autostart:default"
  ]
}
